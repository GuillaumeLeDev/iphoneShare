services:
  photo-server:
    build: .
    network_mode: host
    volumes:
      - /home/guillaume/Downloads:/downloads
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      - DISPLAY=${DISPLAY:-:0}
    env_file:
      - .env
    restart: always
